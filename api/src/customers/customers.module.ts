import { Module } from '@nestjs/common';

// Stub for Phase 1 foundation — controller/service/DTOs to be added
// per the sprint plan (see architecture doc section 4).
@Module({})
export class CustomersModule {}
